---
title: Search Datastore Records
openapi: "POST /api/v2/public/datastore/config/{slug}/{datastore_name}/search"
description: Search records in a config datastore.
---

<Warning>
  **Deprecated.** Datastores are read-only views of data written by workflow executions. For API-managed tables you can create, write, and query directly, use [Persistent Tables](/legacy/api-reference/persistent-tables/list-tables) instead.
</Warning>

Search a datastore's records by column values. The request body is an arbitrary `{column_name: value}` map; values are **regex-matched** against the stored records. Returns the matching records.

<ResponseExample>
```json 200
[
  { "id": "rec_001", "email": "jane@example.com", "stage": "qualified" }
]
```
```json 404
{
  "message": "Server Error",
  "error": "Config not found"
}
```
</ResponseExample>

<RequestExample>
```bash cURL
curl -X POST "https://app.refold.ai/api/v2/public/datastore/config/{slug}/{datastore_name}/search" \
  -H "x-api-key: YOUR_API_KEY" \
  -H "linked_account_id: YOUR_LINKED_ACCOUNT_ID" \
  -H "Content-Type: application/json" \
  -d '{ "stage": "qualified" }'
```
</RequestExample>
