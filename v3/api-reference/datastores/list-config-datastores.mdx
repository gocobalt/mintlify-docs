---
title: List Datastores
api: get /api/v2/public/datastore/config/:slug
description: List the datastore names in an app's config.
---

<Warning>
  **Deprecated.** Datastores are read-only views of data written by workflow executions. For API-managed tables you can create, write, and query directly, use [Persistent Tables](/v3/api-reference/tables/list-tables) instead.
</Warning>

List the datastores available in an app's config for a linked account. Datastores are populated by workflow executions (a workflow writes records to a named datastore); this returns the names you can then read or search.

<Note>
  If no workflow has written to a datastore yet, this returns an empty array `[]`.
</Note>

## Authentication

Datastores are scoped to a linked account via the `linked_account_id` header.

<ParamField header="x-api-key" type="string" required>Your Refold API key.</ParamField>
<ParamField header="linked_account_id" type="string" required>The linked account.</ParamField>

## Path Parameters

<ParamField path="slug" type="string" required>The app slug. **Example:** `hubspot`</ParamField>

## Query Parameters

<ParamField query="config_id" type="string">Config to read from. Defaults to the linked account ID.</ParamField>

## Response

<Tabs>
  <Tab title="200 OK">
    Returns an array of datastore name strings.
  </Tab>

  <Tab title="404 Not Found">
    <ResponseField name="message" type="string">Error message, e.g. `Server Error`.</ResponseField>
    <ResponseField name="error" type="string">Error detail, e.g. `Config not found`.</ResponseField>
  </Tab>
</Tabs>

<ResponseExample>
```json 200
["contacts", "synced_deals"]
```
```json 200 Empty
[]
```
```json 404
{
  "message": "Server Error",
  "error": "Config not found"
}
```
</ResponseExample>

<RequestExample>
```bash cURL
curl -X GET "https://app.refold.ai/api/v2/public/datastore/config/{slug}" \
  -H "x-api-key: YOUR_API_KEY" \
  -H "linked_account_id: YOUR_LINKED_ACCOUNT_ID"
```
</RequestExample>
