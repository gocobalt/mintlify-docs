---
title: Update Config Field Value
openapi: "PUT /api/v2/public/config/field/{fieldId}"
description: Set the value of a single config field.
---

Set the value for a single config field, optionally scoped to a specific workflow. The config must already be installed for the linked account (see [Get or Create Config](/v3/api-reference/configs/create-config)).

<ResponseExample>
```json 200
{
  "id": "699fec7a5bd27ed2af7243ad",
  "name": "Select",
  "field_type": "select",
  "multiple": true,
  "options": [
    { "name": "A", "value": "A" },
    { "name": "B", "value": "B" },
    { "name": "C", "value": "C" }
  ],
  "value": ["A", "B"]
}
```
```json 404
{
  "status_code": 404,
  "http_error_type": "NOT_FOUND",
  "error_code": "SERVER_ERROR",
  "message": "Config with config id user_12345 not found"
}
```
</ResponseExample>

<RequestExample>
```bash cURL (API key)
curl -X PUT "https://app.refold.ai/api/v2/public/config/field/{fieldId}" \
  -H "x-api-key: YOUR_API_KEY" \
  -H "linked_account_id: YOUR_LINKED_ACCOUNT_ID" \
  -H "slug: {slug}" \
  -H "Content-Type: application/json" \
  -d '{ "value": ["A", "B"] }'
```
```bash cURL (session token)
curl -X PUT "https://app.refold.ai/api/v2/public/config/field/{fieldId}" \
  -H "Authorization: Bearer YOUR_SESSION_TOKEN" \
  -H "slug: {slug}" \
  -H "Content-Type: application/json" \
  -d '{ "value": ["A", "B"] }'
```
</RequestExample>
