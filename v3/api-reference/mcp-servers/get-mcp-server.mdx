---
title: Get MCP Server
api: get /api/v2/public/mcp-servers/{mcp_server_id}
description: Get the full configuration of an MCP server.
---

Retrieve the full configuration of an MCP server — its associated apps, chains, suggestions, and notebooks.

## Authentication

This endpoint is scoped to a linked account, so it accepts **either** authentication method.

**Option 1 — API key**

<ParamField header="x-api-key" type="string">Your Refold API key.</ParamField>
<ParamField header="linked_account_id" type="string">The linked account. Required with API-key auth.</ParamField>

**Option 2 — Session token**

<ParamField header="Authorization" type="string">Session token. **Format:** `Bearer YOUR_SESSION_TOKEN`</ParamField>

## Path Parameters

<ParamField path="mcp_server_id" type="string" required>The MCP server ID.</ParamField>

## Response

<Tabs>
  <Tab title="200 OK">
    <ResponseField name="_id" type="string">Server ID.</ResponseField>
    <ResponseField name="name" type="string">Server name.</ResponseField>
    <ResponseField name="description" type="string">Server description.</ResponseField>
    <ResponseField name="associated_apps" type="array">Apps exposed by the server.</ResponseField>
    <ResponseField name="chains" type="array">Configured chains (agents).</ResponseField>
    <ResponseField name="suggestions" type="array">Prompt suggestions.</ResponseField>
    <ResponseField name="notebooks" type="array">Associated notebooks.</ResponseField>
    <ResponseField name="environment" type="string">`test` or `live`.</ResponseField>
    <ResponseField name="createdAt" type="string">ISO 8601 timestamp.</ResponseField>
  </Tab>

  <Tab title="404 Not Found">
    <ResponseField name="status_code" type="integer">HTTP status code.</ResponseField>
    <ResponseField name="http_error_type" type="string">Error category.</ResponseField>
    <ResponseField name="error_code" type="string">Internal error code.</ResponseField>
    <ResponseField name="message" type="string">Human-readable error description.</ResponseField>
  </Tab>
</Tabs>

<ResponseExample>
```json 200
{
  "_id": "6a314ee8d0aeb8165beacc6e",
  "name": "Production CRM Server",
  "description": "MCP server for CRM workflows",
  "associated_org_id": "6500a1b2c3d4e5f6a7b8c9d0",
  "environment": "test",
  "associated_apps": [],
  "chains": [],
  "suggestions": [],
  "notebooks": [],
  "createdAt": "2026-06-16T13:22:00.000Z",
  "updatedAt": "2026-06-16T13:22:00.000Z"
}
```
```json 404
{
  "status_code": 404,
  "http_error_type": "NOT_FOUND",
  "error_code": "SERVER_ERROR",
  "message": "MCPEntry not found"
}
```
</ResponseExample>

<RequestExample>
```bash cURL (API key)
curl -X GET "https://app.refold.ai/api/v2/public/mcp-servers/{mcp_server_id}" \
  -H "x-api-key: YOUR_API_KEY" \
  -H "linked_account_id: YOUR_LINKED_ACCOUNT_ID"
```
```bash cURL (session token)
curl -X GET "https://app.refold.ai/api/v2/public/mcp-servers/{mcp_server_id}" \
  -H "Authorization: Bearer YOUR_SESSION_TOKEN"
```
</RequestExample>
