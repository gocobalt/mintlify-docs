---
title: Add Columns
openapi: "PATCH /api/v2/public/tables/{table_id}/columns/add"
description: Add columns to a persistent table.
---

Add one or more columns to a persistent table. Returns the table's full column list after the change.

<ResponseExample>
```json 200
{
  "columns": ["email", "stage", "score"]
}
```
```json 404
{
  "status_code": 404,
  "http_error_type": "NOT_FOUND",
  "error_code": "SERVER_ERROR",
  "message": "Table not found"
}
```
</ResponseExample>

<RequestExample>
```bash cURL
curl -X PATCH "https://app.refold.ai/api/v2/public/tables/{table_id}/columns/add" \
  -H "x-api-key: YOUR_API_KEY" \
  -H "linked_account_id: YOUR_LINKED_ACCOUNT_ID" \
  -H "Content-Type: application/json" \
  -d '{ "columns": ["score"] }'
```
</RequestExample>
