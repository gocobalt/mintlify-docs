---
title: "Connect from agent code"
description: "Wire a Refold MCP Server URL into the major agent SDKs"
---

A Refold Server URL is everything an agent SDK needs. The URL embeds the auth token in its path, so most SDKs require no extra headers, OAuth flow, or bearer config. Paste the URL, the SDK handles the rest.

Each snippet links to the SDK's MCP documentation so you can confirm the API surface against the version you're running.

<Tabs>
  <Tab title="Anthropic Messages API">
    Use the `mcp_servers` parameter on `messages.create` with the `mcp-client-2025-11-20` beta header. Every server in `mcp_servers` must be referenced by an `mcp_toolset` entry in `tools`.

    ```python
    import anthropic

    client = anthropic.Anthropic()

    response = client.beta.messages.create(
        model="claude-opus-4-7",
        max_tokens=1024,
        messages=[{"role": "user", "content": "What tools do you have?"}],
        mcp_servers=[{
            "type": "url",
            "name": "refold",
            "url": "https://<domain>/mcp/v1/<token>/<server_id>",
        }],
        tools=[{"type": "mcp_toolset", "mcp_server_name": "refold"}],
        betas=["mcp-client-2025-11-20"],
    )
    ```

    | Detail | Value |
    |---|---|
    | Package | `anthropic` (Python SDK) |
    | Endpoint | `client.beta.messages.create` |
    | Beta header | `anthropic-beta: mcp-client-2025-11-20` |
    | Auth | Token is in the URL path; no `authorization_token` field needed |

    Source: [Anthropic MCP connector docs](https://platform.claude.com/docs/en/docs/agents-and-tools/mcp-connector).
  </Tab>

  <Tab title="OpenAI Agents SDK">
    Use `MCPServerStreamableHttp` from `agents.mcp`. Pass the server instance via the `mcp_servers` list on `Agent(...)`.

    ```python
    import asyncio
    from agents import Agent, Runner
    from agents.mcp import MCPServerStreamableHttp

    async def main():
        async with MCPServerStreamableHttp(
            name="Refold",
            params={"url": "https://<domain>/mcp/v1/<token>/<server_id>"},
        ) as refold:
            agent = Agent(
                name="Assistant",
                instructions="Use Refold tools to answer.",
                mcp_servers=[refold],
            )
            result = await Runner.run(agent, "List my available tools.")
            print(result.final_output)

    asyncio.run(main())
    ```

    | Detail | Value |
    |---|---|
    | Package | `openai-agents` (Python) |
    | Class | `MCPServerStreamableHttp` from `agents.mcp` |
    | Transport | Streamable HTTP, auto-selected from `params.url` |
    | Auth | URL path token; no headers needed. For bearer-token servers, add `"headers": {"Authorization": f"Bearer {token}"}` to `params`. |

    Source: [OpenAI Agents SDK MCP docs](https://openai.github.io/openai-agents-python/mcp/).
  </Tab>

  <Tab title="Vercel AI SDK">
    Use `createMCPClient` from `@ai-sdk/mcp` with `transport: { type: 'http' }`. Pass the resulting tools into `streamText` or `generateText`.

    ```ts
    import { createMCPClient } from '@ai-sdk/mcp';
    import { streamText } from 'ai';
    import { anthropic } from '@ai-sdk/anthropic';

    const mcpClient = await createMCPClient({
      transport: {
        type: 'http',
        url: 'https://<domain>/mcp/v1/<token>/<server_id>',
      },
    });

    const tools = await mcpClient.tools();

    const result = await streamText({
      model: anthropic('claude-sonnet-4-5'),
      tools,
      prompt: 'List my recent records.',
      onFinish: async () => { await mcpClient.close(); },
    });
    ```

    | Detail | Value |
    |---|---|
    | Package | `@ai-sdk/mcp` (with `ai`) |
    | Function | `createMCPClient` (stable, not `experimental_`) |
    | Transport | `'http'` (Streamable HTTP) — also supports `'sse'` and `'stdio'` |
    | Auth | URL path token. For header-based servers, pass `headers` inside the transport config. |

    Always `await mcpClient.close()` when the stream finishes to release the connection.

    Source: [Vercel AI SDK MCP tools docs](https://ai-sdk.dev/docs/ai-sdk-core/mcp-tools).
  </Tab>

  <Tab title="LangChain">
    Use `MultiServerMCPClient` from `langchain-mcp-adapters`. Transport string for streamable HTTP is `"http"` (not `"streamable_http"`).

    ```python
    import asyncio
    from langchain_mcp_adapters.client import MultiServerMCPClient
    from langchain.agents import create_agent

    async def main():
        client = MultiServerMCPClient({
            "refold": {
                "transport": "http",
                "url": "https://<domain>/mcp/v1/<token>/<server_id>",
            }
        })
        tools = await client.get_tools()
        agent = create_agent("openai:gpt-4.1", tools)
        result = await agent.ainvoke(
            {"messages": [{"role": "user", "content": "List available tools"}]}
        )
        print(result)

    asyncio.run(main())
    ```

    | Detail | Value |
    |---|---|
    | Package | `langchain-mcp-adapters` (Python) |
    | Class | `MultiServerMCPClient` |
    | Transport key | `"http"` |
    | Auth | URL path token. For header-based servers, add a `"headers"` entry alongside `transport` and `url`. |

    The snippet is async — run it inside an event loop. You'll also need `langchain`, `langgraph`, and a model provider package (e.g., `langchain-openai`) installed.

    Source: [langchain-mcp-adapters README](https://github.com/langchain-ai/langchain-mcp-adapters).
  </Tab>

  <Tab title="Mastra">
    Use `MCPClient` from `@mastra/mcp`. Mastra auto-detects Streamable HTTP from the presence of a `url` (vs. a `command` for stdio).

    ```ts
    import { Agent } from "@mastra/core/agent";
    import { MCPClient } from "@mastra/mcp";
    import { openai } from "@ai-sdk/openai";

    const mcp = new MCPClient({
      servers: {
        refold: {
          url: new URL("https://<domain>/mcp/v1/<token>/<server_id>"),
        },
      },
    });

    export const agent = new Agent({
      name: "refold-agent",
      model: openai("gpt-4o"),
      instructions: "You can use Refold integration tools.",
      tools: await mcp.getTools(),
    });
    ```

    | Detail | Value |
    |---|---|
    | Package | `@mastra/mcp` (with `@mastra/core`) |
    | Class | `MCPClient` |
    | Transport | Streamable HTTP first, falls back to SSE if it fails |
    | Auth | URL path token. For header-based servers, add `requestInit: { headers: {...} }` to the server config. |

    Source: [Mastra MCP docs](https://mastra.ai/docs/agents/using-tools-and-mcp).
  </Tab>
</Tabs>

## Common patterns

### One URL per end user

Refold issues a unique Server URL per linked account. The pattern in production is to provision a Refold MCP server per end customer (or per linked account), store the URL alongside the customer record, and inject it into the agent SDK at request time:

```python
# Pseudocode — same shape across all SDKs
url = db.fetch_refold_url(customer_id)
agent = build_agent_with_mcp(url)
```

### Rotating tokens

If you regenerate a linked account's token in the Refold dashboard, the old URL stops working immediately. Fetch the latest URL from the dashboard before reconstructing the agent, or store the URL with an expiry and refresh on rotation.

## Next steps

<CardGroup cols={2}>
  <Card title="Tools" icon="wrench" href="/v3/mcp-ai-agents/tools-reference/tools">
    What each MCP tool returns once your agent is connected
  </Card>
  <Card title="MCP Logs" icon="clipboard-list" href="/v3/mcp-ai-agents/security/mcp-logs">
    Trace every call your agent makes
  </Card>
</CardGroup>
