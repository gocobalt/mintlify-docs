---
title: "Examples"
description: "Multi-step processes exposed as a single MCP tool"
---

## SAP procurement

An agent needs to handle "Buy a laptop from Dell for $1,200." This is an 8-step process in SAP.

<Tabs>
  <Tab title="Agent calling raw APIs">
    The agent has to figure out and execute all 8 steps:

    1. Validate vendor master data and compliance status
    2. Check budget availability and get approval
    3. Create purchase requisition with cost center
    4. Route through approval hierarchy
    5. Generate purchase order with release strategy
    6. Notify vendor with requirements
    7. Process goods receipt with three-way matching
    8. Handle exceptions (price variance, quality, delays)

    Each step is a decision the agent has to remember. Skipping the compliance check, missing the approval routing, or mis-ordering steps creates real business risk.
  </Tab>

  <Tab title="Agent calling workflows">
    The same process, split into 3 workflows exposed as MCP tools:

    **Workflow 1: Vendor and Budget Validation**
    - Input: Vendor ID, material, quantity, expected price
    - Handles: vendor compliance, budget check, pre-approval
    - Defined error paths: vendor not found, budget insufficient, compliance issues

    **Workflow 2: PO Creation and Routing**
    - Input: Validated vendor data, budget approval, delivery requirements
    - Handles: PR creation, approval routing, PO generation
    - Defined error paths: approval rejection, budget changes, delivery conflicts

    **Workflow 3: Goods Receipt and Exceptions**
    - Input: PO number, delivery details, actual quantities and prices
    - Handles: three-way matching, invoice verification, quality inspection
    - Defined error paths: price variance, quality issues, delivery delays

    The agent calls 3 tools in sequence. Each workflow handles its own complexity internally.
  </Tab>
</Tabs>

Each workflow runs to completion deterministically. The agent receives a structured result and either continues to the next workflow or surfaces the failure to the user.

## When to split a process across multiple workflows

A single workflow can handle an arbitrary number of steps, but splitting can make sense when:

- The agent needs to make a decision between steps (e.g., wait for user confirmation after step 1 before triggering step 2)
- Different parts of the process have different owners or change frequencies
- One part of the process is reusable across multiple flows

When all the steps must run as a single atomic unit, keep them in one workflow.

## Next steps

<CardGroup cols={2}>
  <Card title="Reliability" icon="shield-check" href="/v3/mcp-ai-agents/workflow-as-mcp/reliability">
    What workflows guarantee that raw orchestration cannot
  </Card>
  <Card title="Configuration" icon="gear" href="/v3/mcp-ai-agents/workflow-as-mcp/configuration">
    Attach a workflow to your MCP server
  </Card>
</CardGroup>
