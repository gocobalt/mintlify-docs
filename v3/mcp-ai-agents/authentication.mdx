---
title: "Authentication"
description: "Authenticate agents to an MCP server with per-user tokens, control which apps and actions they reach, and revoke access on demand."
---

MCP authentication builds on Refold [linked accounts](/v3/developer/configure/linked-account). Every MCP server connection carries a token embedded in the Server URL that identifies one linked account, and Refold validates it on every request. The same model that connects your customers' third-party accounts also authenticates their agents — see [Linked accounts](/v3/developer/configure/linked-account) for how those accounts and their credentials are established.

This page covers what is MCP-specific: the Server-URL token, how access is scoped to the apps and actions you expose, and how to revoke a token.

## How authentication works

An MCP client (Claude, Cursor, your own agent) connects using a Server URL that contains an auth token. The token resolves to a single linked account, and that identity governs everything the agent can see and do for the rest of the request.

```mermaid
%%{init: {'theme':'base','themeVariables':{'primaryColor':'#E2E8F0','primaryTextColor':'#1E293B','primaryBorderColor':'#64748B','lineColor':'#64748B','secondaryColor':'#E2E8F0','tertiaryColor':'#F1F5F9','tertiaryTextColor':'#1E293B'}}}%%
flowchart LR
    A["MCP Client<br/>(Claude, Cursor)"] -->|"Server URL with token"| B["Refold MCP Server"]
    B -->|"Validate token"| C["Refold Identity"]
    C -->|"org_id<br/>linked_account_id<br/>environment"| B
    B -->|"Per-request<br/>session context"| D["Tool Execution"]
    D -->|"Stored credentials<br/>(never exposed)"| E["Third-party App"]
```

The agent never handles third-party credentials. Refold resolves the linked account's stored OAuth tokens and API keys internally when a tool executes, so credentials stay encrypted at rest and are never returned in tool inputs or outputs. For where those credentials come from, see [Connectors](/v3/developer/guide/build/connectors/overview); for how end users authorize them, see the [hosted auth flow](/v3/embedded/frontend/hosted-flow).

## The Server-URL token

| Property | Detail |
|----------|--------|
| **Token type** | Linked account token (opaque, server-generated) |
| **Validation** | Every request is validated against Refold |
| **Identity resolution** | Token resolves to `org_id` + `linked_account_id` + `environment` |
| **Token scope** | One token per linked account per MCP server |
| **Invalid token** | Returns HTTP 403 immediately — no tool listing, no partial access |
| **Validation unavailable** | Returns HTTP 503 — fails closed, never falls back to unauthenticated access |

The token state is checked on every request, so there is no cached validation window.

<Warning>
The Server URL contains the auth token. Treat it as a secret: store it like an API key and rotate it the same way. A leaked Server URL grants the same access as the token it carries.
</Warning>

### Issue one URL per end user

Each token is scoped to a single linked account, so issue a separate Server URL for each end user rather than sharing one across users. Two users connecting to the same MCP server with different tokens see the same tool set but operate on their own credentials and data — there is no cross-account access within a session.

<Note>
Per-user tokens are what make revocation precise. When you issue one URL per user, cutting off a single user never affects anyone else connected to the same server.
</Note>

## Control which apps and actions an agent can reach

A token authenticates the agent; the MCP server's configuration decides what that agent can do. Set access on the server itself rather than in the agent.

| Control | How |
|---------|-----|
| **Which apps an agent can access** | Add or remove apps on the MCP server's **Applications** tab |
| **Which actions are available** | Select specific actions per app when adding it to the server |
| **Direct vs agent mode** | Toggle **Agent Mode** to control whether agents see raw tools or resolve-then-execute |
| **Skill access** | Toggle **Retrieve Skill** to control whether agents can discover and load skills |
| **Per-user scoping** | Each linked account gets a unique token, so each user connects with a different URL |

For where these toggles live in the dashboard, see [Server configuration](/v3/mcp-ai-agents/overview/server-configuration). For how the modes change what the agent sees, see [How it works](/v3/mcp-ai-agents/overview/architecture).

## Revoke access

When a Server URL leaks or you need to cut off a user, revoke at the linked-account level.

<Steps>
  <Step title="Regenerate the token to invalidate one URL">
    Regenerate the linked account token in the Refold dashboard. The old token becomes invalid immediately, and every request using it returns HTTP 403. Issue the new Server URL to the user to restore access.
  </Step>
  <Step title="Delete the linked account to revoke everywhere">
    Delete the linked account to revoke all of that end user's tokens across every MCP server they were connected to.
  </Step>
</Steps>

Because token state is checked on every request, revocation takes effect on the next call — there is no cached validation window to wait out.

## See also

- [Linked accounts](/v3/developer/configure/linked-account) — the account and credential model MCP authentication builds on
- [Connectors](/v3/developer/guide/build/connectors/overview) — where the underlying OAuth and API-key credentials are configured
- [Hosted auth flow](/v3/embedded/frontend/hosted-flow) — how end users connect their accounts in the first place
- [Server configuration](/v3/mcp-ai-agents/overview/server-configuration) — where to set the app, action, and mode toggles
- [Security & compliance](/v3/mcp-ai-agents/implementation/security-compliance) — tenant isolation, transport security, and data handling
- [MCP Logs](/v3/mcp-ai-agents/security/mcp-logs) — the audit trail for every authenticated tool call
