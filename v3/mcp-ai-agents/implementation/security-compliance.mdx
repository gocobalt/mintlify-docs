---
title: "Security & Compliance"
description: "How Refold MCP isolates tenants, secures transport, and handles data on managed infrastructure."
---

This page documents how Refold MCP isolates tenants, secures connections, and handles data. It is intended for security reviews, compliance evaluations, and architecture assessments.

For how agents authenticate and how you scope and revoke their access, see [Authentication](/v3/mcp-ai-agents/authentication).

## Tenant isolation

MCP servers are multi-tenant by design. Isolation is enforced at multiple layers:

| Layer | Mechanism |
|-------|-----------|
| **Request identity** | Session context is resolved per-request from the validated token. No shared mutable state across sessions. |
| **Tool resolution** | Tools are resolved per-request based on the authenticated identity. Two concurrent sessions never share tool objects. |
| **Execution context** | Each tool call reads `org_id`, `linked_account_id`, and `environment` from the per-request session context. These fields are immutable for the duration of the request. |
| **Data** | Audit records, tool outputs, and execution metadata are partitioned by `org_id`. Queries are always scoped. |

<Info>
The MCP server is stateless. Identity and tool state are resolved on every request and never cached across requests, so concurrent sessions cannot leak into each other.
</Info>

## Transport security

| Property | Detail |
|----------|--------|
| **Protocol** | MCP v2025-11-25 over Streamable HTTP |
| **Encryption** | TLS 1.2+ for all connections (agent-to-Refold, internal Refold traffic, and Refold-to-third-party apps) |
| **Transport mode** | Stateless HTTP (no persistent WebSocket connections) |

## Data handling

Refold manages OAuth tokens and API keys for connected applications on behalf of linked accounts. Third-party credentials are stored encrypted at rest and are never exposed in MCP tool inputs, outputs, or audit logs. MCP tool handlers never receive raw third-party credentials — Refold resolves them internally when an action executes.

## Audit logging

Every MCP tool call is logged automatically, and logging cannot be disabled. See [MCP Logs](/v3/mcp-ai-agents/security/mcp-logs) for what is captured, the audit guarantees, and how to filter and search records.

## Deployment

Refold MCP servers run on Refold's managed infrastructure.

## See also

- [Authentication](/v3/mcp-ai-agents/authentication) — the Server-URL token, access scoping, and revocation
- [MCP Logs](/v3/mcp-ai-agents/security/mcp-logs) — the audit trail for every tool call
- [How it works](/v3/mcp-ai-agents/overview/architecture) — how a single tool call flows end to end
