---
title: "Examples"
description: "Concrete skill examples you can adapt to your own MCP server"
---

Three example skills showing different patterns. The actions referenced in each example are real Refold action IDs — copy the structure, adapt the field names to your data.

<Note>
**Naming convention.** Most Refold connectors use `snake_case` action IDs (`create_contact`, `get_purchase_orders`). NetSuite uses `camelCase` (`createAccount`, `getVendorById`). Use the exact ID for each app.
</Note>

## Lead qualification (Salesforce)

A single-app skill that combines a SOQL lookup, a conditional create, and scoring.

```markdown
## Goal
Qualify an inbound lead and capture the result on a Salesforce contact.

## Pre-checks
- Email address is provided
- Salesforce connection is active

## Steps

1. Look up the contact in Salesforce.
   Use action `get_entity_with_soql` with:
   `SELECT Id, AccountId, OwnerId FROM Contact WHERE Email = :email LIMIT 1`

2. If a contact exists, check for open opportunities on its account:
   Action `get_entity_with_soql` with:
   `SELECT Id, StageName FROM Opportunity WHERE AccountId = :accountId AND IsClosed = false`

3. If no contact exists, call `create_contact` with:
   `{ FirstName, LastName, Email, Phone, additional_fields: { LeadSource: "Webinar" } }`

4. Score the lead in the agent layer based on company size:
   > 500 employees → "high"
   200–500 → "medium"
   < 200 → "low"

5. Persist the score by updating the contact: action `update_contact` with
   `{ Id: <contactId>, additional_fields: { Lead_Score__c: <score> } }`.

## On success
Return the contact ID, qualification score, and any open opportunity IDs.

## On failure
If Salesforce is unreachable, return an error asking the user to retry.
```

---

## Cross-app contact sync (Salesforce → HubSpot)

A two-app skill. Reads from one system, writes to another, handles existing records idempotently.

```markdown
## Goal
Mirror a Salesforce contact into HubSpot. Update if it already exists.

## Pre-checks
- Salesforce contact ID is provided
- Both Salesforce and HubSpot are connected on this server

## Steps

1. Fetch the contact from Salesforce. Use action `get_entity_with_soql`:
   `SELECT Id, FirstName, LastName, Email, Phone, Account.Name FROM Contact WHERE Id = :sfdcContactId`

2. Look up the contact in HubSpot by email:
   Action `search_contacts` with the email filter.

3. If found, call `update_contact` with the HubSpot contact ID and the
   mapped fields from step 1.

4. If not found, call `create_contact` with the mapped fields.

5. Return both the Salesforce and HubSpot contact IDs in the result.

## Field mapping

| Salesforce | HubSpot |
|------------|---------|
| FirstName  | firstname |
| LastName   | lastname |
| Email      | email |
| Phone      | phone |
| Account.Name | company |

## On failure
- If Salesforce lookup fails: stop, return the error to the user.
- If HubSpot is unreachable: do not retry indefinitely. Return the error
  with the partial data fetched from Salesforce.
```

---

## Purchase order with vendor validation (SAP S/4HANA)

A skill with strict ordering and explicit failure paths. Use this pattern for processes where a wrong step has downstream cost.

```markdown
## Goal
Create a purchase order in SAP S/4HANA after validating the supplier.

## Pre-checks
- Supplier business partner ID, material, quantity, and expected price are provided
- SAP S/4HANA connection is active

## Steps

1. Validate the supplier by calling `get_business_partner_by_id` with the
   provided BusinessPartner ID. Confirm the record exists and check the
   relevant role/blocking fields in the response.

2. (Optional) List recent purchase orders for the supplier to surface any
   existing in-flight POs the user should be aware of: action
   `get_all_purchase_orders` with a `$filter` like
   `Supplier eq '<id>' and PurchaseOrderType eq 'NB'`.

3. Create the PO via `create_purchase_order` with the header and item
   payload required by SAP S/4HANA (PurchaseOrderType, CompanyCode,
   PurchasingOrganization, items, etc.).

4. Return the new PurchaseOrder ID, supplier, and total value.

## On failure
- Supplier not found: stop at step 1. Return the error with the
  BusinessPartner ID the user provided.
- PO creation fails: do not retry — the user needs to see the error to
  decide whether to fix the input.
```

---

These examples show three patterns: single-app lookup-and-act, cross-app sync, and strict business-logic enforcement. Start by copying the closest shape and adapting the actions to your own apps.

To find action IDs for an app on your server, browse the Applications tab in the dashboard — every action exposed to the agent has its slug visible.

## Next steps

<CardGroup cols={2}>
  <Card title="Creating Skills" icon="pen" href="/v3/mcp-ai-agents/skills/creating-skills">
    Fields, content structure, and the runbook pattern
  </Card>
  <Card title="Tools" icon="wrench" href="/v3/mcp-ai-agents/tools-reference/tools">
    The tools agents use to discover and load skills
  </Card>
</CardGroup>
