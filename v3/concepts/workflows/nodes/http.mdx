---
title: "HTTP Node"
description: "Make HTTP requests to external APIs with configurable methods, headers, authentication, and pagination support"
sidebarTitle: "HTTP"
---

The HTTP node enables you to make HTTP requests to external REST APIs and webhook endpoints during workflow execution. It supports all standard HTTP methods, custom headers, query parameters, request bodies, and built-in pagination handling for APIs that return paginated responses.

## When to use

- Call external APIs to fetch or send data during workflow execution
- Integrate with third-party services that expose REST endpoints
- Send webhook notifications to external systems
- Retrieve additional data from microservices based on workflow context
- Interact with enterprise systems (SAP, NetSuite, Tipalti) via their REST APIs

<Info>For file transfers over SFTP or large file operations, use the [File Handler node](/v3/concepts/workflows/nodes/file-handler) instead.</Info>

## Input parameters

### Request configuration

<ParamField path="Select HTTP Method" type="dropdown" required>
  HTTP method for the request. Options: `GET`, `POST`, `PUT`, `DELETE`, `PATCH`.
</ParamField>

<ParamField path="Set Base URL" type="string">
  The base URL of the API endpoint. Example: `https://api.example.com`
</ParamField>

<ParamField path="Set URL Path" type="string">
  The API path appended to the base URL. Supports dynamic variables. Example: `/v1/users` or `/vendors/{{vendor_id}}`
</ParamField>

### Query params

Add query parameters as key-value pairs. Click the **+** button to add additional parameters.

| Field | Description |
|-------|-------------|
| Name | Parameter name. Example: `limit`, `page`, `filter` |
| Value | Parameter value. Supports dynamic variables. Example: `10`, `{{page_number}}` |

### Headers

Add HTTP headers as key-value pairs. Click the **+** button to add additional headers.

| Field | Description |
|-------|-------------|
| Name | Header name. Example: `Authorization`, `Content-Type`, `X-API-Key` |
| Value | Header value. Supports dynamic variables. Example: `Bearer {{api_token}}`, `application/json` |

### Body

<ParamField path="Set Request Body" type="code editor">
  Request body for POST, PUT, and PATCH requests. Enter JSON, XML, or other formats as required by the target API. Supports dynamic variables using `{{variable}}` syntax.
</ParamField>

<ParamField path="Set Request Body Type" type="dropdown">
  Format of the request body. Options: `JSON`, `HTML`, `XML`, `TEXT`, `Form Data`, `X-WWW-FORM-URLENCODED`.
</ParamField>

<Info>The Body field only appears when the method is set to POST, PUT, or PATCH.</Info>

### Settings

<ParamField path="Use mTLS" type="boolean">
  Enable mutual TLS authentication for secure API connections. When enabled, select the certificate source.
</ParamField>

<ParamField path="Certificate Source" type="toggle">
  Choose between `truststore` or `certificate` as the source for mTLS credentials.
</ParamField>

<ParamField path="Select Trust Store / Select Certificate Store" type="dropdown">
  Select the configured trust store or certificate store from your organization settings. Configure stores under [**Settings > Organization > Certificates**](/v3/platform-settings/workspace-settings/certificates).
</ParamField>

<ParamField path="Use Server Trust Store" type="boolean">
  Use the server's trust store for SSL certificate validation.
</ParamField>

### Pagination

<ParamField path="type" type="dropdown">
  Select pagination handling method for APIs that return paginated responses. Options: `No pagination`, `Page Based`, `Offset Based`, `Cursor Based`, `Time Based`, `SQL Query Based`, `Custom`.
</ParamField>

<AccordionGroup>
  <Accordion title="Pagination Types">
    | Type | Description | Key fields |
    |------|-------------|------------|
    | No pagination | Single request, no automatic pagination | — |
    | Page Based | Increments a page number to fetch subsequent pages | Page Key, Per Page Limit Key, Limit (default 50), Start Page, Max End Page, and Total Page Path *or* Response records Path |
    | Offset Based | Uses offset + limit to page through records | Limit Key, Offset Key, Limit (default 100), and Total Records Path *or* Response records Path |
    | Cursor Based | Follows cursor tokens returned by the API | Next Page Cursor Path |
    | Time Based | Paginates across timestamp ranges | — |
    | SQL Query Based | Builds a SQL-style query that advances a start position by a limit | Query (Primary Clause, Start Position, Limit, Max Records, Batch Size, Order of Start and Limit) |
    | Custom | Define custom pagination logic | — |
  </Accordion>
</AccordionGroup>

<Info>For Page, Offset, Cursor, and SQL Query pagination, you also choose where the pagination value is sent on each request via **Inject to** — `No Injection`, `Request Body`, `Request Headers`, or `Request Query Parameters`.</Info>

### Response

<ParamField path="Response Type" type="dropdown">
  How the response is returned. Options: `Default`, `Stream`.
</ParamField>

<ParamField path="Use Response Bucket" type="boolean">
  When enabled, the response is stored in the response bucket and returned as a hosted file rather than inline.
</ParamField>

<ParamField path="Bucket Object Settings" type="object">
  Storage settings for the response file when **Use Response Bucket** is enabled — Infinite Expiry, TTL, and Preserve Cloud Provider Presigned URL.
</ParamField>

## Output

The node returns the complete API response. The output structure depends on the target API's response format.

**Success Response Example:**
```json
{
  "status": 200,
  "headers": {
    "content-type": "application/json",
    "x-request-id": "abc123"
  },
  "body": {
    "id": "12345",
    "name": "Acme Corporation",
    "status": "active"
  }
}
```

**Error Response Example:**
```json
{
  "status": "Errored",
  "node_id": "16",
  "node_name": "Send GET request",
  "body": {
    "message": "Invalid URL",
    "error": {
      "code": "ERR_INVALID_URL",
      "input": "//jsonplaceholder.typicode.com/todos/1"
    },
    "status": 500
  }
}
```

## Adding to your workflow

<Steps>
  <Step title="Add the HTTP node">
    In the workflow editor, click **Add Node** and select **HTTP Node** from the utility nodes section.
  </Step>
  
  <Step title="Select HTTP method">
    Choose the appropriate method from the dropdown:
    - **GET** for retrieving data
    - **POST** for creating resources
    - **PUT/PATCH** for updating resources
    - **DELETE** for removing resources
  </Step>
  
  <Step title="Configure URL">
    Enter the base URL and path for your API endpoint. Use dynamic variables to include data from previous nodes.
  </Step>
  
  <Step title="Add headers and parameters">
    Switch between the **Query Params** and **Headers** sub-tabs to configure:
    - Authentication headers (Bearer tokens, API keys)
    - Content-Type headers
    - Query parameters for filtering or pagination
  </Step>
  
  <Step title="Configure body (if applicable)">
    For POST, PUT, or PATCH requests, enter the request body in JSON format. Reference data from previous nodes using `{{node_name.field}}` syntax.
  </Step>
  
  <Step title="Test the node">
    Click the **Run** tab to execute a test request and verify the response structure.
  </Step>
</Steps>

## Examples

<AccordionGroup>
  <Accordion title="Fetch Vendor from NetSuite">
    Retrieve vendor details from NetSuite using their REST API.

    **Configuration:**

    | Field | Value |
    |-------|-------|
    | Method | `GET` |
    | Base URL | `https://{{netsuite_account_id}}.suitetalk.api.netsuite.com` |
    | Path | `/services/rest/record/v1/vendor/{{vendor_internal_id}}` |

    **Headers:**

    | Name | Value |
    |------|-------|
    | Authorization | `Bearer {{netsuite_token}}` |
    | Content-Type | `application/json` |

    **Output:**
```json
    {
      "status": 200,
      "body": {
        "id": "12345",
        "entityId": "VEND-001",
        "companyName": "Acme Supplies",
        "email": "accounts@acme.com",
        "subsidiary": {
          "id": "1",
          "refName": "Parent Company"
        }
      }
    }
```
  </Accordion>

  <Accordion title="Create SAP Business Partner">
    Create a new business partner in SAP S/4HANA via OData API.

    **Configuration:**

    | Field | Value |
    |-------|-------|
    | Method | `POST` |
    | Base URL | `https://{{sap_host}}/sap/opu/odata/sap` |
    | Path | `/API_BUSINESS_PARTNER/A_BusinessPartner` |

    **Headers:**

    | Name | Value |
    |------|-------|
    | Authorization | `Basic {{sap_credentials}}` |
    | Content-Type | `application/json` |
    | X-CSRF-Token | `{{csrf_token}}` |

    **Body:**
```json
    {
      "BusinessPartnerCategory": "2",
      "BusinessPartnerFullName": "{{company_name}}",
      "BusinessPartnerIsBlocked": false,
      "Industry": "{{industry_code}}",
      "LegalForm": "{{legal_form}}"
    }
```

    **Output:**
```json
    {
      "status": 201,
      "body": {
        "d": {
          "BusinessPartner": "1000001",
          "BusinessPartnerFullName": "Tech Solutions Inc",
          "CreationDate": "/Date(1705276800000)/"
        }
      }
    }
```
  </Accordion>

  <Accordion title="Submit Payment to Tipalti">
    Submit a payment request to Tipalti's payment API.

    **Configuration:**

    | Field | Value |
    |-------|-------|
    | Method | `POST` |
    | Base URL | `https://api.tipalti.com` |
    | Path | `/v1/payments` |

    **Headers:**

    | Name | Value |
    |------|-------|
    | Authorization | `Bearer {{tipalti_api_key}}` |
    | Content-Type | `application/json` |
    | Idempotency-Key | `{{idempotency_key}}` |

    **Body:**
```json
    {
      "payeeId": "{{vendor_id}}",
      "amount": {{payment_amount}},
      "currency": "USD",
      "description": "Invoice {{invoice_number}}",
      "paymentMethod": "ACH"
    }
```

    **Output:**
```json
    {
      "status": 200,
      "body": {
        "paymentId": "PAY-78901",
        "status": "pending",
        "payeeId": "VEND-456",
        "amount": 5000.00,
        "currency": "USD",
        "estimatedArrival": "2024-01-20"
      }
    }
```
  </Accordion>

  <Accordion title="GET with Pagination (Offset Based)">
    Fetch all invoices from an API using offset-based pagination.

    **Configuration:**

    | Field | Value |
    |-------|-------|
    | Method | `GET` |
    | Base URL | `https://api.accounting-system.com` |
    | Path | `/v2/invoices` |
    | Pagination Type | `Offset Based` |

    **Query Params:**

    | Name | Value |
    |------|-------|
    | status | `unpaid` |
    | date_from | `{{start_date}}` |

    **Output (aggregated from all pages):**
```json
    {
      "status": 200,
      "body": {
        "invoices": [
          { "id": "INV-001", "amount": 1500.00 },
          { "id": "INV-002", "amount": 2300.00 },
          { "id": "INV-003", "amount": 890.00 }
        ],
        "total": 3
      }
    }
```
  </Accordion>
</AccordionGroup>

## Troubleshooting

<AccordionGroup>
  <Accordion title="Connection Errors">
    | Problem | Solution |
    |---------|----------|
    | Invalid URL error | Verify the base URL includes the protocol (`https://`). Check for typos in the URL or path. |
    | Connection timeout | Verify the target API is accessible. Check network connectivity and firewall rules. |
    | DNS resolution failed | Verify the hostname is correct. Check if the API endpoint is publicly accessible. |
  </Accordion>

  <Accordion title="Authentication Errors">
    | Problem | Solution |
    |---------|----------|
    | 401 Unauthorized | Verify API credentials are correct and not expired. Check the Authorization header format. |
    | 403 Forbidden | Confirm the API key or token has permissions for the requested resource. |
    | Invalid token format | Ensure Bearer tokens include the `Bearer ` prefix. Verify base64 encoding for Basic auth. |
  </Accordion>

  <Accordion title="Request Errors">
    | Problem | Solution |
    |---------|----------|
    | 400 Bad Request | Verify request body format matches API requirements. Check Content-Type header. |
    | 404 Not Found | Verify the API path is correct. Check if the resource exists. |
    | 405 Method Not Allowed | Verify the HTTP method is supported by the endpoint. |
  </Accordion>

  <Accordion title="mTLS Errors">
    | Problem | Solution |
    |---------|----------|
    | Certificate not found | Verify the trust store or certificate is configured under [**Settings > Organization > Certificates**](/v3/platform-settings/workspace-settings/certificates). |
    | SSL handshake failed | Check certificate validity and expiration. Ensure the certificate matches the API requirements. |
  </Accordion>
</AccordionGroup>

<Tip>Use a [Custom Code node](/v3/concepts/workflows/nodes/custom-code) after the HTTP node to inspect and transform response data before passing it to subsequent nodes.</Tip>

## Next steps

<CardGroup cols={2}>
  <Card title="Transform node" icon="shuffle" href="/v3/concepts/workflows/nodes/transform">
    Restructure API response data using JSONata.
  </Card>
  <Card title="Rule node" icon="bolt" href="/v3/concepts/workflows/nodes/rule">
    Add conditional logic based on HTTP response status.
  </Card>
  <Card title="Loop node" icon="repeat" href="/v3/concepts/workflows/nodes/loop">
    Iterate over paginated results.
  </Card>
  <Card title="Data Mapper node" icon="diagram-project" href="/v3/concepts/workflows/nodes/data-mapper">
    Map API response fields to your internal schema.
  </Card>
</CardGroup>