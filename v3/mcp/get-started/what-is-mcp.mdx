---
title: "What is MCP"
description: "Give AI agents access to 300+ enterprise integrations through hosted MCP servers"
---

The [Model Context Protocol](https://modelcontextprotocol.io) is an open standard for connecting AI agents to external systems and data. Refold MCP lets SaaS platforms expose their integrations to AI agents over MCP. You configure an MCP server in the Refold dashboard, connect the enterprise apps your customers use, and hand them a URL. Their AI agents connect to that URL and get access to integration actions they can discover and call.

No custom integration code. No agent framework. Refold handles authentication, execution, tenant isolation, and audit logging.

<CardGroup cols={3}>
  <Card title="300+ Connectors" icon="plug">
    Salesforce, SAP, NetSuite, Workday, HubSpot, and hundreds more
  </Card>
  <Card title="Full Audit Trail" icon="clipboard-list">
    Every tool call logged with identity, input, output, and timing
  </Card>
  <Card title="Tenant Isolation" icon="shield">
    Per-request identity resolution. No shared state across sessions.
  </Card>
</CardGroup>

## What the agent can do

Once connected, an agent can:

- **Call integration actions** directly: create records, query data, update fields across connected apps
- **Run workflows** as single tool calls with built-in error handling and retry logic
- **Discover skills** to find pre-built procedures for complex multi-step operations
- **Work across multiple apps** in a single session — for example, read from Salesforce and write to SAP

Every tool call is authenticated and logged in [MCP Logs](/v3/mcp/operate/mcp-logs). For how the server resolves identity and executes each call, see [How it works](/v3/mcp/get-started/architecture).

## Patterns

A server can expose integrations to an agent in three ways. Most servers combine them.

| Pattern | Best for |
|---------|----------|
| **Direct** | Simple CRUD — each action becomes its own tool |
| **Agent + Skills** | Multi-step tasks where you want guidance but flexibility |
| **Workflows as tools** | Business-critical processes that run server-side as one call |

See [Choose your pattern](/v3/mcp/get-started/choose-your-pattern) for the decision framework.

## Next steps

<CardGroup cols={3}>
  <Card title="Quickstart" icon="play" href="/v3/mcp/get-started/getting-started">
    Set up your first MCP server and connect from Claude or Cursor
  </Card>
  <Card title="Connect from agent code" icon="code" href="/v3/mcp/connect/connect-from-agent-code">
    Wire the Server URL into Anthropic, OpenAI Agents, Vercel AI, LangChain, or Mastra
  </Card>
  <Card title="Choose your pattern" icon="route" href="/v3/mcp/get-started/choose-your-pattern">
    Direct, skills, or workflows — pick how much guidance your agent gets
  </Card>
  <Card title="How it works" icon="sitemap" href="/v3/mcp/get-started/architecture">
    How the server processes requests and executes actions
  </Card>
  <Card title="MCP Logs" icon="clipboard-list" href="/v3/mcp/operate/mcp-logs">
    Audit trail for every tool call
  </Card>
</CardGroup>
